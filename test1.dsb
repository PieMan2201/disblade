DISPLAY 8,1
STORAGE 6,6
STORAGE(0,1) from 100
STORAGE(0,0) from TIME
EVALUATE STORAGE
STORAGE(2,0) from STORAGE(0,2)
STORAGE(4,0) from 0
STORAGE(4,1) from 1
WHL(STORAGE(0,0)<STORAGE(2,0)){
 STORAGE(0,0) from TIME
 EVALUATE STORAGE
 STORAGE(4,0) from STORAGE(4,2)
}
DISPLAY(0,0) from STORAGE(4,2)
EVALUATE DISPLAY
HALTCODE